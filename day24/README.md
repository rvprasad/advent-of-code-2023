Execute this code via `elixir day24.exs <filename>`.

### Observations

1. To output multiple values for the purpose of debugging, provide a list of
   these values as the only argument to `IO.inspect`.
